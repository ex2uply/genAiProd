name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-lab1:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab1
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 1 tests
      working-directory: ./lab1
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 1 pipeline
      working-directory: ./lab1
      run: |
        python pipeline.py
        ls -la output/

  test-lab3:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab3
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 3 tests
      working-directory: ./lab3/pipeline
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 3 pipeline
      working-directory: ./lab3/pipeline
      run: |
        python main.py
        ls -la ../output/

  test-lab2:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Install dependencies
      working-directory: ./lab2
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 2 tests
      working-directory: ./lab2/pipeline
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 2 pipeline
      working-directory: ./lab2/pipeline
      run: |
        python main.py
        ls -la ../output/

  test-lab4:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab4
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 4 tests
      working-directory: ./lab4/banking_pipeline
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 4 pipeline
      working-directory: ./lab4/banking_pipeline/src
      run: |
        python main.py
        ls -la ../output/

  test-lab5:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab5
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 5 tests
      working-directory: ./lab5
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 5 pipeline
      working-directory: ./lab5
      run: |
        python pipeline.py
        ls -la output/

  test-lab6:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab6
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 6 tests
      working-directory: ./lab6
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 6 pipeline
      working-directory: ./lab6
      run: |
        python pipeline.py
        ls -la output/

  test-lab7:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Install dependencies
      working-directory: ./lab7
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 7 tests
      working-directory: ./lab7
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 7 optimized pipeline
      working-directory: ./lab7
      run: |
        python pipeline.py
        ls -la output/

  test-lab8:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      working-directory: ./lab8
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Lab 8 tests
      working-directory: ./lab8
      run: |
        python -m pytest tests -v
    
    - name: Run Lab 8 pipeline
      working-directory: ./lab8
      run: |
        python pipeline.py
        ls -la output/

  test-case-study:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Install dependencies
      working-directory: ./case-study
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Case Study tests
      working-directory: ./case-study
      run: |
        python -m pytest tests -v
    
    - name: Run Case Study medallion pipeline
      working-directory: ./case-study/pipeline
      run: |
        python main.py
        ls -la ../output/

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install linting tools
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black
    
    - name: Lint with flake8
      run: |
        # Stop the build if there are Python syntax errors or undefined names
        flake8 lab1 lab2 lab3 lab4 lab5 lab6 lab7 lab8 case-study --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings
        flake8 lab1 lab2 lab3 lab4 lab5 lab6 lab7 lab8 case-study --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Check formatting with black
      run: |
        black --check lab1 lab2 lab3 lab4 lab5 lab6 lab7 lab8 case-study || true
